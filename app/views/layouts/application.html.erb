<html lang="en">
  <head>
    <%= render 'shared/head' %>
  </head>
  <body>
    <%= render 'shared/menu' %>
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-2">

        </div>
        <div class="col-md-10">
          <div id="notes">
            <% if flash %>
              <%= render 'shared/flash' %>
            <% end %>
          </div>

          <%= yield %>
        </div>
      </div>
      <footer id="site-footer" class="row">
      </footer>
    </div>
    <%= render 'shared/after_footer' %>
  </body>
</html>
