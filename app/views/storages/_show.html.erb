<div class="modal-dialog" role="document">
  <div class="modal-content card">
    <h4 id="storage-modal-label" class="card-header bg-info">
      <%= Storage.model_name.human %>
      <span class="tag tag-default">ID: <%= @storage.id %></span>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </h4>
    <div class="card-block">
      <div class="container-fluid">
        <%= render partial: 'storages/inner_show', locals: { storage: @storage, native: true } %>
      </div>
    </div>
    <div class="card-footer bg-info">
      <button type="button" class="btn btn-sm btn-info pull-right" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
        Close
      </button>
    </div>
  </div>
</div>
