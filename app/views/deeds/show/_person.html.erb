<li class="list-group-item">
  <% if person.gender == 'unknown' %>
    <%= icon('question', class: 'fa-fw') %>
  <% else %>
    <%= icon(person.gender, class: 'fa-fw') %>
  <% end %>
  <%= person.name %>
  <span class="pull-right">
    <%= link_to_show_entity person %>
  </span>
</li>
